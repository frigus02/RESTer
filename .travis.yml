dist: trusty
sudo: false
addons:
  firefox: "latest-dev"
language: node_js
node_js: node
script:
- yarn build
- yarn lint
- yarn test
cache:
  yarn: true
  directories:
  - node_modules
  - $HOME/.cache/bower
